# General Instructions

## GitHub
### Add Your SSH Key to your SSH Agent
Enable the SSH agent:
```shell
eval "$(ssh-agent -s)"
```
Add Your SSH Key to your SSH Agent
```shell
ssh-add ~/.ssh/id_rsa
```
## Angular

## Windows
